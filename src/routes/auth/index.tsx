import { component$ } from "@builder.io/qwik";
import AuthDialog from "~/routes/auth/auth-dialog";

export default component$(() => {
  return (
    // <h1>Login screen placeholder</h1>
    <AuthDialog/>
  )
})